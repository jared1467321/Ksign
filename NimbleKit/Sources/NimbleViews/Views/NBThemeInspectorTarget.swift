// Theme inspection now lives in NimbleExtensions so every NBHalloween call
// site can use the same registration and exact-element override APIs without a
// dependency on NimbleViews. This source file intentionally remains as a stub
// to keep the package layout/source history stable.
